type: object
properties:
  target_branch_protection:
    $ref: "#/$defs/rule"
  source_branch_protection:
    $ref: "#/$defs/rule"
  collaborators:
    $ref: "#/$defs/rule"
  execute_job:
    $ref: "#/$defs/rule"
$defs:
  rule:
    type: object
    properties:
      enabled:
        type: boolean
    required:
      - enabled
